import { styleHookSingleton } from './hook';
export const styleSingleton = () => {
    const useStyle = styleHookSingleton();
    const Sheet = ({ styles }) => {
        useStyle(styles);
        return null;
    };
    return Sheet;
};
